name: Update Stock Candidates V223 Supply

on:
  workflow_dispatch:
  schedule:
    # 평일 한국시간 18:30 실행 = UTC 09:30
    - cron: "30 9 * * 1-5"

permissions:
  contents: write

jobs:
  update-stock-candidates:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show repository files
        run: |
          echo "Repository root:"
          pwd
          echo "Files:"
          ls -la
          echo "Scripts folder:"
          ls -la scripts || true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run V223 supply connector
        run: |
          if [ -f "scripts/update_stock_candidates_v223_supply.py" ]; then
            python scripts/update_stock_candidates_v223_supply.py
          else
            echo "ERROR: scripts/update_stock_candidates_v223_supply.py not found."
            exit 1
          fi

      - name: Commit generated files
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add stock_candidates.json app_data_status.json v223_generation_summary.json v223_collection_errors.txt || true

          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "V223 auto update stock candidates with supply data"
            git push
          fi
